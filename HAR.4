#include <iostream>
#include <cmath>
using namespace std;
double calculateArea(double length, double width) {
    return length * width;
}
double calculateArea(double radius) {
    return M_PI * radius * radius;
}
double calculateArea(double base, double height) {
    return 0.5 * base * height;
}
int main() {
    double lengthRect = 5.0;
    double widthRect = 3.0;
    cout << "Area of rectangle: " << calculateArea(lengthRect, widthRect) << endl;
    double radiusCircle = 4.0;
    cout << "Area of circle: " << calculateArea(radiusCircle) << endl;
    double baseTriangle = 6.0;
    double heightTriangle = 8.0;
    cout << "Area of triangle: " << calculateArea(baseTriangle, heightTriangle) << endl;

    return 0;
}
