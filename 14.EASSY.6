#include <iostream>
using namespace std;

// Function printMessage with a default argument "Hello"
void printMessage(const char* message = "Hello") {
    cout << message << endl;
}

int main() {
    // Call the printMessage function without passing any argument
    printMessage();

    return 0;
}
