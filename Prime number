#include <iostream>
using namespace std;
int main()
{
    int a,i,flag=0;
    cout<<"enter the number: ";
    cin>>a;
    for(i=2;i<a;i++)
    {
        if(a%i==0)
        {
            flag=1;
        }
    }
    if(flag==0)
    {
        cout<<"prime";
    }
    else
    {
        cout<<"not prime";
    }
}
